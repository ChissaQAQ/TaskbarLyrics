# 更新日志

版本号规则：`1.0.x`，x 每次发布递增。日期为发布日。
`v1.0.18` 不存在（打包过程中跳号）。

## 1.0.25 — 2026-08-15

这一版修三个**看起来像随机、其实都有确定成因**的问题：歌词抓成 Live 版、
自动避让停在一个早已不存在的旧位置、以及设置一重启电脑就变回去。

### 修复

- **同一首歌抓到 Live 版的歌词**：候选择优原先只比「有译文的行数」，
  于是长一倍的 Live 版光靠行数多就能赢过录音室版——实测「居眠り遠征隊」录音室版
  45 行里 43 行有译文，Live 版 102 行里 100 行有译文，抓回来整首都是 Live 的即兴口白。
  改为**歌名分数优先、覆盖率用比率**（本意只是「别挑到残缺翻译」，比率就够用）
  - 平时兜底的两道闸在网易云上双双失效，所以只能靠排序本身站住：网易云的 SMTC
    完全不上报 timeline，`durationS` 恒为 0，「时长差」和「歌词比歌长」两处判断直接短路
  - 提前收工的条件也跟着收紧成「歌名满分**且**译文全覆盖」：Live 版也可能全覆盖，
    先返回就再也轮不到正确的那首
- **自动避让把窗口停在陈旧位置、和图标之间空一块**：UI 侧「拿不到新快照就沿用旧空档」
  这个策略本身是对的（UIA 抖一下就让窗口跳位更难看），但它原先**没有任何年龄上限**，
  而后台枚举有三条静默停产的路子（异常被吞、返回空树不覆盖旧快照、UIA 同步跨进程调用
  explorer 且没有超时）。一旦踩上，窗口就无限期停在几天前那份布局算出来的位置上，
  既不自愈也不留一个字的痕迹。实测现场：窗口停在 `1592..1830`，而当时真实空档是
  `1535..1838`，左边凭空空出 57px
  - 加看护：心跳超时判定线程卡在 UIA 调用里出不来时，**换一条线程接手**（挂住的那次
    调用不可取消，旧线程只能弃置，用代号让它返回后自行退场、不许覆盖新快照，
    接手次数封顶 3 次）；线程还在转但持续拿不到结果则拉回快节奏死等恢复，并留一条日志
  - 判定前先确认三轮：`TickCount64` 算的是开机时长而非运行时长，睡眠唤醒后心跳
    看着也像「很久没动」，而线程其实好得很——真卡住的线程叫不动
- **1.0.24 的遗留窗口过滤有个绕过口子**：那道过滤排在「接近全宽的容器继续往下拆」
  之后，而遗留窗口报的矩形本来就跟现实无关，它完全可以报出接近全宽——实测副屏
  `WorkerW` 的 UIA 矩形是 `48..1920`（宽 1872，稳稳越过阈值），而它真实矩形只有
  `385..830`。现在把过滤提到容器分支之前，并补上漏掉的 `MSTaskListWClass`
- **设置在电脑重启后变回旧值**（反馈集中在「避让停靠侧」和「停靠对齐」，实际**所有**
  设置项都会中）：配置的读与写各有一套路径选择逻辑，两套**不对称**。存盘在 exe 同目录
  写不进去时会转存 `%AppData%`，而读取只在 exe 同目录「不存在」文件时才看退路——
  于是存进去的和读回来的根本不是同一个文件，现象就是「改完当场生效、重启后变回去」，
  且全程静默（转存成功不提示、写失败的日志也只记第一次）
  - 触发它**不需要**权限长期不足：OneDrive 同步、杀软扫描、备份工具短暂锁住
    `config.json` 就够了，而路径一旦切到退路本次运行就不再回头，那次抖动之后的
    所有改动都注定在重启时丢失
  - 改为**读写对称**：两处都有内容时取较新的那份，它坏了再退到另一份
  - 那两项之所以被单独报出来，是因为它们的默认值恰好和多数人想要的相反，每次重启都得重设
- **存盘不是原子的**：`File.WriteAllText` 直接覆写，写一半遇上关机/断电就留下半截 JSON，
  而解析失败的代价是整份设置回默认值。改为先写 `.tmp` 再原子改名
  （`LyricsCache` 早就踩过同一个坑，解法也是这个）
- **配置解析失败会静默丢掉全部设置**：原先只是不声不响地回到默认值，而回默认只发生在
  内存里——紧接着任何一次存盘（拖动窗口松手、检查更新写时间戳）就把整份默认值覆盖到
  原文件上，用户攒下的设置从此不可恢复。现在记日志，并把坏文件隔离成 `config.json.bad`
  留证：既不会被覆盖，也让下一次启动有机会退到另一处那份旧配置

### 优化

- **静默降级现在留得下痕迹**：日志原先只能写异常对象，但有些故障不以异常的形式出现
  （某个后台查询从此不再产出结果、配置被转存到了别处），用户看到的只是「位置不对」
  或「设置没了」。新增写普通消息的口子，这类现场事后查得到

## 1.0.24 — 2026-08-06

这一版解决**浅色任务栏上几乎看不见歌词**，以及**自动避让把窗口停到错位置**两件事。

### 修复

- **浅色任务栏上白字几乎看不见**：文字颜色原先固定写死，任务栏一浅就只剩一层白糊。
  新增「文字颜色」选项，**默认跟随系统明暗**：深色任务栏白字、浅色任务栏近黑字，
  系统换主题（含日落自动切换）时自动跟上；想固定颜色的仍可切「自定义」
  - 明暗判据取 `SystemUsesLightTheme` 而非 `AppsUseLightTheme`：这是两个独立的值，
    任务栏的明暗只认前者（实测同一台机器上两者取值可以正好相反）
  - Windows 换深浅色时不给普通窗口发任何收得到的通知，只能轮询——搭在已有的
    1.5 秒任务栏贴合节拍上，读一个注册表值的开销远小于那一轮本来就要做的事
  - **阴影跟着文字反色**：阴影的作用是给笔画描反差，深色文字配黑阴影只会更糊。
    按文字亮度自动选黑/白（自定义颜色同样适用，所以不必再多一个「阴影颜色」开关）
- **浅色任务栏上悬停遮罩与封面占位一起消失**：这两处原先在布局里写死了半透明白——
  压在浅底上等于什么都没画，占位的白音符更是直接看不见。现在也跟着任务栏明暗切两套
  （淡白 ↔ 淡黑），与 Win11 任务栏图标的悬停高亮保持一致
- **自动避让把窗口停到错位置**：Win11 把任务栏整个换成了 XAML，但 Win10 时代的容器
  窗口还挂在树上，报着一个跟实际布局毫无关系的旧矩形，且 `IsOffscreen=False`、
  `IsWindowVisible=True`——两种可见性判据都过滤不掉它。Win11 图标居中、图标一少
  真图标区就往中间收缩，这块不动的旧矩形会凭空吃掉几百像素可用区
  （主屏是 `ReBarWindow32`，副屏任务栏是 `WorkerW`）。改为按系统版本 + 类名跳过：
  这两个类在 Win10 上是包着整个任务列表的真实容器，光看类名就跳会连里面的按钮一起漏
- **窗口硬挤在窄的那半边被切字**：「避让停靠侧」原先是硬约束，指定的半边只要有一条缝
  就往里挤，哪怕对面宽出一大截。实测过右半边只有 255px、左半边有 337px，
  窗口硬缩到 247px，长歌名当场被裁掉，看着像被旁边的图标压住了。
  现在这个偏好在「连最小可读宽度都放不下」时转为软约束，对面宽出 40px 以上才跨过去
  （留门槛是为了避免两边宽度稍有变化窗口就来回搬家）

### 优化

- **空档太窄时先收封面、把地方全让给文字**：内容区窄到 160 DIP 以下时长歌词只剩
  几个字在那滚，读不成句子，此时宁可把封面收掉
- **与两侧图标的间距 4px → 8px**：原先贴得太近，长歌词滚动的裁切边缘正好压在图标边上，
  看起来像被图标遮住了
- **设置页「外观」加颜色预设**：原文/译文各 4 个常用色块，点一下填进输入框；
  跟随系统时输入框与色块置灰——留着能点却不生效最气人

## 1.0.23 — 2026-08-06

### 修复

- **「检查更新」失败时只说「检查失败，请稍后重试」**：限流、断网、被拦截、真故障
  全归成同一句话，异常还被就地吞掉、`error.log` 里一个字都不留，
  用户看不出原因，反馈时也拿不出线索。现在按原因分开讲：
  - 撞上 GitHub 匿名接口限流 → 明说「每小时 60 次、与同一网络下的其他人共享，
    约 X 分钟后恢复」。这个配额**按出口 IP 计**，公司 / 学校 / 宿舍共用出口时
    是几个人分摊同一份，撞上属于正常现象，不该让人以为程序坏了
  - 连不上 → 「连不上 GitHub（网络不通或被拦截）」
  - 404 / 5xx / 其他 → 各自的说法，意料之外的异常记入 `error.log`
  - 限流判据取 `X-RateLimit-Remaining` 归零而非单看 403：凭证问题也会返 403，
    混在一起会给出错误的解释

### 优化

- **启动检查加节流**：距上次成功检查不足 6 小时就跳过。新版一天也发不了几个，
  而那 60 次配额是按出口 IP 共享的，一天开关十几次机器就能耗掉一截，
  真撞上限流反而查不到更新。时间戳只在成功时写入（否则一次限流会把接下来
  6 小时的自动检查全掐掉）；手动点「检查更新」不受节流限制
- **新增 `--update-test` 诊断入口**：跑一遍真实的检查流程，打印结果与剩余配额，
  再遇到「检查失败」可以直接看出是限流、断网还是真故障

## 1.0.22 — 2026-08-06

这一版的重点全在**歌词内容的正确性**和**切行动画的观感**上。

### 修复

- **有些歌只有中文译文、没有原文**：这类投稿把中文翻译直接填进了网易云的主歌词字段、
  `tlyric` 留空，原文只存在于酷狗 KRC 那侧。此时文本比对必然全线落空
  （日文比中文，52 行只凑巧配上 3 行），既反转不了主从也挂不上逐字。
  改为在文本对不上时补一条判据完全不同的通路：靠**语言构成**与**时间轴逐行贴合度**
  认出「主歌词是译文、原文在 KRC」这种情形，认出后原文上行、中文下行、全曲逐字
  （实测 7co「ハツコイノウタ」：修复前 52 行全中文 / 6% 逐字，修复后 45 行原文 + 44 行译文 / 100% 逐字）
- **歌词抓错**：候选筛选改为「歌名必须命中」+ 按**译文覆盖率**择优，
  不再出现「歌名对不上但歌手对得上」的另一首歌被选中
- **一句译文对着两句原文**：KRC 逐字把一行拆成两行时，译文原先只挂到前半句，
  后半句下面是空的。改为按时间**向后吸附**找回被拆走的那半句，并同步搬运译文
- **首句没有译文**：首句的译文被前置的歌名/作者信息行抢走
- **切换到罗马音后第二行完全空白**：抓取时没请求罗马音字段（`rv`），
  切过去既没有罗马音也没有译文
- **中间几句突然丢译文**：原文与译文行数不齐时留下的空隙，改为用相邻行**夹逼**填补
- **快结束时又跳回开头第一句**：长尾奏（长器乐尾奏、Live 版）被误判成单曲循环。
  启发式余量从 20s 放宽到 45s，宁可漏判一次重播也不错判
- **长时间运行后任务栏卡死、程序自行退出**：任务栏元素枚举（UIA）搬到独立后台线程，
  并修掉原生内存泄漏；三处未捕获异常加兜底日志
- **设置窗口始终置顶**：改为普通窗口，可以被其他窗口正常盖住
- **非歌词行漏过滤**：作词/作曲/编曲/制作人等 credits 行的识别加强
- **设置页「关于」的更新源说明过时**：更新源已换成 GitHub Releases，但这句用户可见的
  说明仍写着「连的是内网 Gitea，不在内网时会失败」，把能用的功能描述成用不了的

### 优化

- **切行动画重做**：位移换用**正弦缓出**并把幅度压到行高的 0.6。
  原先位移和淡变共用三次缓出，初速度是平均速度的 3 倍 —— 开头几帧每帧跨 6~7px，
  在 12pt 小字上就是半个字高，随后飞快衰减，观感是「猛地一冲再拖着尾巴黏过去」。
  新旧两行现在共用同一条曲线和同一幅度，看起来是一条传送带在动
- **动画期间不再收窄窗口**：分层窗口每次改宽度都要重建整张 surface 并让 explorer
  重合成任务栏，而这一下原先精确落在动画第 0 帧。变宽仍然立刻（否则新行会被裁掉尾巴），
  收窄推迟到动画跑完 —— 那 320ms 里右侧多几像素透明空白没人看得出来
- **歌词磁盘缓存**：抓到的歌词与逐字表按歌落盘（30 天 / 上限 400 首），
  切歌瞬间出词，断网时听过的歌照样有歌词，也少打三家曲库的公开接口
- **繁简与别名归一化**：歌名歌手的繁简变体、多歌手写法不再漏歌
- **逐字歌词改以 KRC 文本为显示主体**：有逐字数据的歌，整首歌每一句都能逐字显示，
  不再出现「同一首歌有的句子有逐字、有的没有」
- **托盘菜单加歌词偏移快捷调整**：不用开设置窗口就能微调超前/滞后
- **省掉一处无谓计算**：没有逐字数据时按行时长合成的匀速进度，
  原先每 50ms 重算一遍（结果绝大多数随即被丢弃），改为按行缓存

### 变更

- **自动更新源换成 GitHub Releases**（原先是作者的内网 Gitea，不在该内网时必然失败）

## 1.0.21 — 2026-08-04

- 修歌词抓错与设置页明暗配色（Win10 上文字与背景同色导致看不见）
- 设置窗口新增「关于」页（版本、项目地址）
- README 补充 SMTC 开关说明

## 1.0.20 — 2026-07-31

- 修长时间运行导致任务栏卡死与程序自行退出
- 仓库精简为纯 WPF 版，移除早期 Python 实现

## 1.0.0 – 1.0.19 — 2026-07-29 ~ 07-30

初版成型期，主要节点：

- `1.0.0` WPF（.NET 10）版初始提交
- `1.0.1` 右键菜单换成 WPF Fluent 样式
- `1.0.2` 设置界面重构为 Win11 风格 + 修居中对齐锚点
- `1.0.4` 设置界面 Win11 保真度：下拉框全套模板 + Mica 背景
- `1.0.6` 无歌词时两行显示（歌名 + 歌手）
- `1.0.7` 悬停遮罩按实际文字宽度收 + 浮动窗半透明底色
- `1.0.9` 新增自动避让任务栏元素（可开关）
- `1.0.11` 自动避让改填充式语义 + UIA 容错
- `1.0.12` Win10 兼容 + 中文歌传送带修重影 + 加粗原文选项
- `1.0.14` 新增停靠对齐选项（左 / 右 / 居中）
- `1.0.15` 新增检查更新与一键自动更新
- `1.0.16` 修超长歌词滚动尾部不显示（根因：TextBlock 按测量宽度排版）
- `1.0.17` 提高歌词匹配精确度，歌名作为候选必要条件
- `1.0.19` 长歌词滚动改帧驱动平滑 + 修所有动画缺 From 导致的失效
